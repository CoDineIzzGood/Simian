# screen_recorder.py — lightweight stub (no-op) for packaged demo builds
def start_recording(duration=60, include_audio=True):
    print(f"[screen_recorder] Stub: would record for {duration}s (audio={include_audio})")